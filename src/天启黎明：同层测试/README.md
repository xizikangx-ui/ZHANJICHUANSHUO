# 天启黎明：同层测试

双模式入口：`index.ts`

- 在酒馆中：自动挂载为同层流式界面（每层可直接输入并拿到 AI 回复）。
- 在普通网页中：直接作为独立聊天页运行（`index.html + index.js`）。

本地调试导入（酒馆脚本）：

```ts
import 'https://raw.githubusercontent.com/xizikangx-ui/ZHANJICHUANSHUO/main/dist/天启黎明：同层测试/index.js'
```
